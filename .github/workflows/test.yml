name: Test Project
on: [push]

jobs:
  test-wf:
    runs-on: ubuntu-latest
    steps:
      - name: Get code
        uses: actions/checkout@v3
      - name: Install Dps
        run: npm ci
      - name: Run test
        run: npm test
